#include <stdio.h>
#include <stdlib.h>

    int main()
    {
     int qtd = 2 ,taxa;
     double salario;

     printf("digite quantidade ");
     scanf("%d", &qtd);

     printf(" Digite a Taxa");
     scanf("%d", &taxa);

     printf("Digite o salario");
     scanf("%lf", &salario);

     printf("Digite o salario");
     scanf("%lf", &salario);

        return 0;
    }
