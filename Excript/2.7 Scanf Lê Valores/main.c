#include <stdio.h>
#include <stdlib.h>


int main()

{

    int qtd = 2 , taxa;
    double salario;
    scanf("%d", & qtd);                                 // armazena QTD digitado no teclado
    scanf("%d", & taxa);
    scanf("%lf", & salario);
    scanf("%lf", & salario);

    printf("Quantidade: %d", qtd);                      // imprime QTD na tela
    printf(" Taxa %d", taxa);
    printf("Salario : %lf", salario);
    printf("Salario: %lf", salario);


    return 0;
}
