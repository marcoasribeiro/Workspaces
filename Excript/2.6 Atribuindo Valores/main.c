#include <stdio.h>          // bibliotecas
#include <stdlib.h>

int main()
{
    int x , y = 3 ;             // variavel  inteiro
    double r = 12.3, s;         // variavel double
    x = 2;                         // inteiro
    y =  x +32;                     // variavel
    s = x +r;

    scanf("%d", x);                 // le o teclado
    printf("Soma: %d ", y );        //imprime na tela
    return 0;
}
